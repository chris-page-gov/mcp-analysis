# TypeScript Tooling

TypeScript utilities belong here. Run them with `ts-node` or through compiled output as needed. Build tooling using pnpm scripts:

```bash
pnpm typecheck
pnpm lint
```

Add new dependencies to `package.json` and re-run `pnpm install` inside the dev container.
